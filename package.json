{
  "name": "pidanvoca",
  "version": "1.1.0",
  "private": true,
  "description": "Offline vocabulary flashcards with FSRS review scheduling",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build-vocabulary.js",
    "test": "node --test \"tests/*.test.js\"",
    "test:unit": "node --test \"tests/*.test.js\""
  },
  "dependencies": {
    "ts-fsrs": "5.4.1"
  }
}
