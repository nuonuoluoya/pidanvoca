{
  "name": "pidanvoca",
  "version": "1.1.0",
  "private": true,
  "description": "Offline vocabulary flashcards with FSRS review scheduling",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build-vocabulary.js",
    "build:all": "node build-vocabulary.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test",
    "test:unit": "node --test",
    "test:e2e": "playwright test",
    "perf:check": "node scripts/performance-budget.js",
    "release:check": "npm run check && npm run perf:check && npm run test:e2e && npm run ci:generated",
    "check": "npm run lint && npm run format:check && npm run typecheck && npm test && npm run build",
    "ci:generated": "npm run build && git diff --exit-code -- vocabulary-flashcards.html data dist"
  },
  "dependencies": {
    "ts-fsrs": "5.4.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "eslint": "^10.8.1",
    "fake-indexeddb": "^6.2.5",
    "globals": "^17.11.0",
    "prettier": "^3.9.6",
    "typescript": "^7.0.2"
  }
}
