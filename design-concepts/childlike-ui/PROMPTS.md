# Image Gen prompt set

All six images use the user-provided reference as a **style reference**, not as an edit target. Shared direction: `ui-mockup`, tactile cream-paper scrapbook, hand-drawn marker accents, candy-colored pill controls, sticker icons, layered paper shadows, readable Chinese typography, and an implementation-friendly layout. Shared palette: cream `#FFF8E7`, ocean blue `#64BCEB`, coral `#EA6D78`, sunshine yellow `#F4C84D`, mint `#A9D66F`, lavender `#B995D6`, cocoa text `#5C3A45`.

## desktop-classic.png

16:10 desktop primary classic-study screen for “随机单词本”. Show a centered stacked vocabulary card, progress `8 / 30`, word `water`, phonetic, Chinese meaning, tense information, sound/dictionary/study-mode controls, top-right review/settings controls, previous/next arrows, and layered translucent cut-paper ocean waves filling the lower 38% without covering text. Use restrained clouds, ocean doodles, paper boat, bubbles, stitching, tape, and pencil accents. Avoid generic SaaS styling, glassmorphism, neon, dark mode, clutter, or unrelated sections.

## desktop-settings.png

16:10 desktop settings state in the same visual system. Keep a partial word card on the left and show the full `页面设置` paper drawer on the right. Include the seven built-in wordbooks and counts, selected `小学英语单词本 502词`, group-size presets and stepper, memory-curve summary and daily-new-word controls, import/export mode, advanced/danger section, shuffle, and vocabulary import actions. Selected and expanded states must be obvious and readable.

## desktop-review-complete.png

16:10 desktop memory-review and completion concept. Main state: `今日复习`, `MEMORY REVIEW`, `3 / 10`, `今日新词`, progress bar, undo/close, word `kind`, sound, recall prompt, answer, `忘记 10分钟`, and `记得 2天`. Secondary state: dimmed backdrop with `本组完成`, `30 / 30`, detail copy, `再来一组`, and `调整数量`.

## mobile-classic.png

9:16 portrait mobile classic-study screen with no device frame. Include title, review/settings controls, tall stacked cream card, `8 / 30`, `water`, phonetic, meaning, tense line, sound/dictionary/study-mode controls, thumb-reachable previous/next arrows, and layered translucent blue cut-paper waves filling the lower 43% while keeping all text readable.

## mobile-settings.png

9:16 portrait mobile settings drawer occupying about 82% of the viewport with the underlying word card dimmed and visible. Include `页面设置`, expanded wordbook list and counts, selected primary-school book, expanded group-size controls, collapsed memory-curve summary, shuffle, and import actions. Optimize for one-handed use and readable touch targets.

## mobile-review-complete.png

9:16 portrait mobile review screen with `今日复习`, `MEMORY REVIEW`, progress, undo/close, `kind`, sound, prompt, answer, and large bottom rating buttons. Include a smaller completion inset for `复习完成` and a labeled `本组完成 30 / 30` preview. Keep the main review state dominant and readable.
